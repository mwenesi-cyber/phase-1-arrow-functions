// 1. Function expression (review)
const divide = function(a, b) {
  return a / b;
};

// 2. Arrow function - one-liner
const square = x => x * x;

// 3. Arrow function - two parameters
const add = (a, b) => a + b;

// 4. Arrow function - no parameters
const greet = () => "Hello, world!";
